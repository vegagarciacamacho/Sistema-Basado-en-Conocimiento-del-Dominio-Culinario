[build-system]
requires = ["setuptools>=61", "setuptools_scm"]
build-backend = "setuptools.build_meta"

[project]
name = "fdi-sbc-2504"
version = "0.1.0"
description = "Sistema basado en conocimiento"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["sbc"]

